require 'spec_helper'

describe MealStatus do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    MealStatus.create!(@valid_attributes)
  end
end
